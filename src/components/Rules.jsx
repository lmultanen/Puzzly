import React from "react";

const Rules = () => {
    return(
        <div>
            {/* <div className="nav-buffer"/> */}
            <p>Placeholder for rules page. Will explain that users should tap on tiles in their Tile Bank and then tap a corresponding slot in the Solve Grid.</p>
            <p>If a user taps on an empty space or their Tile Bank after selecting a tile, that tile will be placed there.</p>
            <p>If a user taps on a tile when a different tile is already selected, those two tiles will change places.</p>
        </div>
    )
}
export default Rules;